# اجرای Pars AI با Docker

این تنظیمات نسخه وب و سرور را اجرا می‌کنند؛ فایل نصب Android تولید نمی‌کنند.

## شروع روی ویندوز

Docker Desktop با موتور Linux containers باید نصب و روشن باشد. داخل پوشه پروژه اجرا کنید:

```powershell
Copy-Item .env.docker.example .env.docker
docker compose --env-file .env.docker up --build -d
```

سپس http://localhost:8080 را باز کنید. ورود نمایشی با کد 1234 است؛ پیامک ارسال نمی‌شود. کاتالوگ و پرامپت‌ساز محلی بدون کلید کار می‌کنند. دریافت آنلاین داده به دسترسی شبکه به سرویس مربوط نیاز دارد.

## وضعیت، گزارش و توقف

```powershell
docker compose --env-file .env.docker ps
docker compose --env-file .env.docker logs --tail=100
docker compose --env-file .env.docker down
```

داده‌های سرور در volume با نام parsai_data باقی می‌مانند. دستور down را با گزینه -v اجرا نکنید مگر قصد حذف داده‌ها را داشته باشید. علاقه‌مندی‌های رابط فعلی در مرورگر همان دستگاه ذخیره می‌شوند.

بعد از تغییر کد، فرمان up --build -d را دوباره اجرا کنید. تغییر زبان و ورود نمایشی در نسخه وب فعال است.

## ارسال به دیگران

آدرس localhost فقط روی دستگاه خودتان قابل استفاده است. برای لینک عمومی، میزبان دارای Docker و دامنه و HTTPS لازم است. این پیکربندی عمداً فقط روی 127.0.0.1 گوش می‌دهد؛ روی سرور، reverse proxy دارای HTTPS را به پورت 8080 متصل کنید.

فایل‌های .env، کلیدها، بانک اطلاعاتی و node_modules را برای دیگران یا GitHub ارسال نکنید. کلیدهای سرویس فقط در .env.docker سرور قرار می‌گیرند و داخل خروجی وب نیستند.

## شرایط انتشار واقعی

برای ورود واقعی EXPO_PUBLIC_DEMO_AUTH=false، APP_ENV=production، یک JWT_SECRET تصادفی حداقل 32 کاراکتری و اطلاعات کاوه‌نگار را تنظیم کنید و دوباره build بگیرید. OPENAI_MODEL و OPENAI_SEARCH_MODEL باید مدل‌های در دسترس حساب API شما باشند. کلید OpenAI فقط روی سرور تنظیم شود. مسیرهای پولی سرور ورود معتبر لازم دارند.

رابط چت فعلی راهنمای محلی است و پرامپت‌ساز فعلی قالب متنی می‌سازد؛ قرار دادن کلید به‌تنهایی این رابط‌ها را به چت مولد تبدیل نمی‌کند. اتصال کامل این رابط‌ها، پرداخت فروشگاه، سیاست حریم خصوصی، حذف حساب، آزمون دستگاه واقعی و APK امضاشده هنوز شرط انتشار تجاری هستند.

Docker یا این فایل‌ها تضمین دسترسی بدون فیلتر، رایگان بودن مدل‌ها یا آماده‌بودن محصول برای فروشگاه نیستند.
